# Herbivora application dependencies (GUI + inference).
#
# PyTorch (torch / torchvision) is NOT listed here on purpose.
# Install it first via Install_Herbivora.bat / Install_Herbivora.command
# (or Install_CPU.bat, Install_CUDA.bat, Install.bat, install.sh)
# so the correct CPU or CUDA wheel is used. See USER_GUIDE.md / INSTALL.md.

opencv-python
numpy>=2.0.0
Pillow
pillow-heif>=0.16.0
scipy
tqdm>=4.65.0
pyyaml
segmentation-models-pytorch
timm
transformers
huggingface_hub
einops
kornia
ultralytics
customtkinter>=5.2.0
